# Code Generated by Sidekick is for learning and experimentation purposes only.
---
- hosts: green
  become: yes
  tasks:
    - name: Deploy Docker container on green environment
      include_role:
        name: docker_deploy

- hosts: localhost
  tasks:
    - name: Switch traffic from blue to green
      include_role:
        name: switch_traffic

- hosts: blue
  become: yes
  tasks:
    - name: Stop old Docker container on blue environment
      docker_container:
        name: app
        state: stopped
